//import React from 'react';
//import ReactDOM from 'react-dom';

//var listOfImages =[];
/*
class ImageApp extends React.Component{
    importAll(r) {
        return r.keys().map(r);
    }
    UNSAFE_componentWillMount() {
        listOfImages = this.importAll(require.context("./TestImages/", false, /\.(png|jpe?g|svg)$/));
    }
    render(){
        return(
          <div>
              {
                    listOfImages.map(
                      (image, index) =>    <img key={index} src={image} alt="-altinfo-"></img>
                    )
              }
          </div>
        )
    }
}
export default ImageApp;*/